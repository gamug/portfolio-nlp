[pytest]
# "src" makes its flat modules (db, pipeline, categories, ...) importable;
# "." lets tests/news_nlp/conftest.py import apps.news_nlp_api from the repo root.
pythonpath = src .
testpaths = tests
asyncio_mode = auto
